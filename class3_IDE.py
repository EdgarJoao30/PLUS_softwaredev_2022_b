import os
import sys

#this is the test for the IDE class, using VS code

print(sys.path)
help("modules")
days_of_the_week = 7
def say_hello(recipient):
    print("Hello world!, {}" .format(recipient))
    return recipient